# Traefik Config Manager - Environment Configuration
#
# Copy this file to .env and adjust values as needed

# Path to Traefik directory on the HOST system
# This is mounted into the container at /traefik
TRAEFIK_DIR=/var/opt/services/traefik
